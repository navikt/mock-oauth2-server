// TODO should issue tokens for whatever query paramsÆ/form params and provide jwks and well-known
// should be able to provide the exact same tokens as the mockoauth2server, could provide input as both java/kotlin and json
// should handle keys etc only